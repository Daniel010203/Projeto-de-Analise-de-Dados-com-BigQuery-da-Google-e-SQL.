Analise de dados com a função COUNT(1), onde trás o resultado de quantas linhas possue a entidade.

SELECT  COUNT(1) id_municipio,
FROM `basedosdados.br_ms_cnes.dados_complementares`
WHERE ano = 2021;
